<div class="col-md-3 mb-4">
	<div class="card">
		<img src={{imageUrl}} class="card-img-top" alt="...">
		<div class="card-body">
			<h5 class="card-title">{{name}}</h5>
			<p>by {{user.username}}</p>
			<p class="card-text">This Deck is the best one so ... ya.</p>
			<a href="/decks/{{id}}" class="btn btn-primary">Open Deck</a>
		</div>
	</div>
</div>