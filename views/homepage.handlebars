<div class="container">
	<div class="d-flex justify-content-center text-success">
		<h3>Latest Decks</h3>
	</div>
	<div class="container">
		<div class="row">
			{{#each decks as |deck|}}

			{{> deck-info deck}}

			{{/each}}
		</div>
	</div>
</div>